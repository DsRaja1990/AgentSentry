# Integration: Microsoft Agent Framework (.NET) — Phase 2 stub

Planned: a sample .NET 8 agent built on Microsoft Agent Framework that routes
its model calls and tool invocations through AgentSentry.

Until the .NET SDK lands, point any OpenAI-compatible HTTP client at the
gateway (see [docs/usage-developer.md](../../docs/usage-developer.md)).
