# Bicep modules — Phase 2 stub

Production Azure deployment (Container Apps + Azure Database for PostgreSQL +
self-hosted ClickHouse on AKS or ClickHouse Cloud connector) will live here.

Until then, the `deploy/docker-compose.dev.yml` definition is the source of
truth for service wiring.
