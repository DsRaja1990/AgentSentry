# Terraform modules — Phase 2 stub

AWS (EKS + RDS Postgres + MSK for span queue + ClickHouse Cloud) and GCP
(GKE + Cloud SQL + ClickHouse on GCE) modules will live here.

For now use `deploy/docker-compose.dev.yml`.
